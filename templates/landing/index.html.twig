{% extends 'base.html.twig' %}

{% block title %}Samaco Brewery - Crafting Excellence{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #e2e8f0;
            overflow-x: hidden;
            background: #0a0a0a;
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 24px;
        }

        /* Typography */
        h1, h2, h3, h4, h5, h6 {
            font-weight: 700;
            line-height: 1.2;
        }

        .section-title {
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .section-subtitle {
            text-align: center;
            color: #9ca3af;
            font-size: 1.125rem;
            max-width: 600px;
            margin: 0 auto 3rem;
        }

        /* Buttons */
        .btn-primary {
            display: inline-block;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            color: #000;
            padding: 12px 28px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(255, 215, 0, 0.3);
        }

        .btn-secondary {
            display: inline-block;
            background: transparent;
            color: #FFD700;
            padding: 12px 28px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            border: 2px solid #FFD700;
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            background: rgba(255, 215, 0, 0.1);
            transform: translateY(-2px);
        }

        /* Navigation */
        .navbar {
            background: linear-gradient(180deg, #000000 0%, #0a0a0a 100%);
            border-bottom: 2px solid #FFD700;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }

        .navbar .container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 80px;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            text-decoration: none;
        }

        .logo img {
            height: 40px;
            width: auto;
        }

        .logo span {
            font-size: 1.25rem;
            font-weight: 700;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .nav-links {
            display: flex;
            gap: 32px;
            align-items: center;
        }

        .nav-links a {
            text-decoration: none;
            color: #e2e8f0;
            font-weight: 500;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: #FFD700;
        }

        .nav-buttons {
            display: flex;
            gap: 12px;
        }

        /* Mobile Menu */
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            cursor: pointer;
            padding: 8px;
            color: #FFD700;
            font-size: 28px;
        }

        .mobile-menu {
            display: none;
            position: fixed;
            top: 80px;
            left: 0;
            right: 0;
            background: #000000;
            border-bottom: 2px solid #FFD700;
            padding: 1rem;
            z-index: 999;
        }

        .mobile-menu.active {
            display: block;
        }

        .nav-links a:hover,
        .nav-links a.active {
            color: #FFD700;
        }

        .mobile-menu a {
            display: block;
            padding: 12px 16px;
            color: #e2e8f0;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
        }

        .mobile-menu a:hover,
        .mobile-menu a.active {
            color: #FFD700;
        }


        /* Hero Section with Background Image */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            position: relative;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(0,0,0,0.20) 0%, rgba(0,0,0,0.15) 100%);
            z-index: 1;
        }

        .hero-bg {
            background-image: url('../images/beer-background.jpg');
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" opacity="0.05"><path fill="%23FFD700" d="M50,10 L60,30 L80,35 L65,50 L70,70 L50,60 L30,70 L35,50 L20,35 L40,30 Z"/></svg>') repeat;
            opacity: 0.1;
            pointer-events: none;
        }

        .hero .container {
            display: flex;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .hero-badge {
            display: inline-block;
            background: rgba(255, 215, 0, 0.15);
            color: #FFD700;
            padding: 6px 16px;
            border-radius: 50px;
            font-size: 0.875rem;
            font-weight: 600;
            margin-bottom: 24px;
            border: 1px solid rgba(255, 215, 0, 0.3);
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 24px;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 50%, #FFD700 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .hero p {
            font-size: 1.125rem;
            color: #9ca3af;
            margin-bottom: 32px;
            line-height: 1.8;
            max-width: 600px;
        }

        .hero-buttons {
            display: flex;
            gap: 16px;
            margin-bottom: 32px;
            flex-wrap: wrap;
        }

        .trust-badge {
            display: flex;
            align-items: center;
            gap: 16px;
            color: #6b7280;
            font-size: 0.875rem;
        }

        .avatars {
            display: flex;
        }

        .avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: 2px solid #FFD700;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 0.75rem;
            color: #000;
            background: #FFD700;
        }

        /* Stats Section */
        .stats {
            background: #0a0a0a;
            padding: 60px 0;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 32px;
            text-align: center;
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 800;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 8px;
        }

        .stat-label {
            color: #9ca3af;
        }

        /* Features Section */
        .features {
            padding: 100px 0;
            background: #000000;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 32px;
            margin-top: 48px;
        }

        .feature-card {
            text-align: center;
            padding: 32px 24px;
            border-radius: 20px;
            background: linear-gradient(135deg, #0a0a0a 0%, #111111 100%);
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 215, 0, 0.1);
        }

        .feature-card:hover {
            transform: translateY(-8px);
            border-color: rgba(255, 215, 0, 0.3);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5);
        }

        .feature-icon {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, rgba(255, 215, 0, 0.1) 0%, rgba(255, 215, 0, 0.05) 100%);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 2rem;
            border: 1px solid rgba(255, 215, 0, 0.2);
        }

        .feature-card h3 {
            margin-bottom: 12px;
            color: #FFD700;
        }

        .feature-card p {
            color: #9ca3af;
            font-size: 0.9375rem;
        }

        /* Products Section */
        .products {
            padding: 100px 0;
            background: linear-gradient(180deg, #0a0a0a 0%, #000000 100%);
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 32px;
            margin-top: 48px;
        }

        .product-card {
            background: linear-gradient(135deg, #0a0a0a 0%, #111111 100%);
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 215, 0, 0.1);
        }

        .product-card:hover {
            transform: translateY(-8px);
            border-color: rgba(255, 215, 0, 0.3);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5);
        }

        .product-image {
            height: 200px;
            background: linear-gradient(135deg, #1a1a1a 0%, #000000 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 4rem;
            border-bottom: 1px solid rgba(255, 215, 0, 0.2);
        }

        .product-content {
            padding: 24px;
        }

        .product-tag {
            display: inline-block;
            background: rgba(255, 215, 0, 0.15);
            color: #FFD700;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            margin-bottom: 16px;
        }

        .product-card h3 {
            margin-bottom: 12px;
            color: #FFD700;
        }

        .product-card p {
            color: #9ca3af;
            font-size: 0.875rem;
            margin-bottom: 16px;
        }

        .product-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 16px;
        }

        .price {
            font-size: 1.5rem;
            font-weight: 700;
            color: #FFD700;
        }

        /* Process Section */
        .process {
            padding: 100px 0;
            background: #000000;
        }

        .process-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 32px;
            margin-top: 48px;
        }

        .process-step {
            text-align: center;
        }

        .step-number {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            color: #000;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0 auto 20px;
            box-shadow: 0 8px 20px rgba(255, 215, 0, 0.3);
        }

        .process-step h3 {
            margin-bottom: 12px;
            color: #FFD700;
        }

        .process-step p {
            color: #9ca3af;
            font-size: 0.9375rem;
        }

        /* Testimonials */
        .testimonials {
            padding: 100px 0;
            background: linear-gradient(180deg, #0a0a0a 0%, #000000 100%);
        }

        .testimonials-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 32px;
        }

        .testimonial-card {
            background: linear-gradient(135deg, #0a0a0a 0%, #111111 100%);
            border-radius: 20px;
            padding: 32px;
            border: 1px solid rgba(255, 215, 0, 0.1);
            transition: all 0.3s ease;
        }

        .testimonial-card:hover {
            border-color: rgba(255, 215, 0, 0.3);
            transform: translateY(-4px);
        }

        .quote-icon {
            color: #FFD700;
            font-size: 3rem;
            line-height: 1;
            margin-bottom: 20px;
            font-family: serif;
        }

        .testimonial-card p {
            color: #d1d5db;
            margin-bottom: 24px;
            line-height: 1.6;
        }

        .testimonial-author {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .author-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            color: #000;
        }

        .author-name {
            font-weight: 600;
            color: #FFD700;
            margin-bottom: 4px;
        }

        .author-title {
            font-size: 0.875rem;
            color: #9ca3af;
        }

        /* Section Separators - Yellow Lines */
        .stats, .features, .products, .process, .testimonials {
            border-bottom: 2px solid rgba(255, 215, 0, 0.25);
            position: relative;
        }

        /* CTA Section */
        .cta {
            padding: 100px 0;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            text-align: center;
        }

        .cta h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #000;
        }

        .cta p {
            font-size: 1.125rem;
            margin-bottom: 32px;
            color: #1a1a1a;
            opacity: 0.9;
        }

        .cta-buttons {
            display: flex;
            gap: 16px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn-white {
            background: #000;
            color: #FFD700;
            padding: 12px 32px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            transition: all 0.3s;
        }

        .btn-white:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .btn-outline-white {
            background: transparent;
            color: #000;
            padding: 12px 32px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            border: 2px solid #000;
            transition: all 0.3s;
        }

        .btn-outline-white:hover {
            background: #000;
            color: #FFD700;
        }

        /* Footer */
        .footer {
            background: #000000;
            color: #9ca3af;
            padding: 60px 0 30px;
            border-top: 2px solid #FFD700;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 48px;
            margin-bottom: 48px;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 16px;
            text-decoration: none;
        }

        .footer-logo span {
            font-size: 1.125rem;
            font-weight: 700;
            background: linear-gradient(135deg, #FFD700 0%, #FFC107 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .footer h4 {
            color: #FFD700;
            margin-bottom: 20px;
            font-size: 1.125rem;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: #9ca3af;
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer-links a:hover {
            color: #FFD700;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 215, 0, 0.2);
            font-size: 0.875rem;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .features-grid,
            .products-grid,
            .process-grid,
            .testimonials-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .hero .container {
                text-align: center;
                justify-content: center;
            }
            
            .hero p {
                margin-left: auto;
                margin-right: auto;
            }
            
            .hero-buttons {
                justify-content: center;
            }
            
            .trust-badge {
                justify-content: center;
            }
            
            .features-grid,
            .products-grid,
            .process-grid,
            .testimonials-grid,
            .footer-grid {
                grid-template-columns: 1fr;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .hero h1 {
                font-size: 2.5rem;
            }
            
            .nav-links {
                display: none;
            }
            
            .mobile-menu-btn {
                display: block;
            }
            
            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
        }

        @media (max-width: 480px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .container {
                padding: 0 16px;
            }
            
            .hero h1 {
                font-size: 2rem;
            }
        }
    </style>
{% endblock %}

{% block body %}
<!-- Navigation -->
<nav class="navbar">
    <div class="container">
        <a href="{{ path('app_landing') }}" class="logo">
            <img src="{{ asset('images/logo.png') }}" alt="Samaco Brewery">
            <span>Samaco Brewery</span>
        </a>
        
        <div class="nav-links">
            <a href="{{ path('app_landing') }}"class="active">Home</a>
            <a href="{{ path('app_about') }}">About</a>
            <a href="{{ path('app_contact') }}">Contact</a>
        </div>
        
        <div class="nav-links">
            <a href="{{ path('app_login') }}" class="btn-secondary">Login</a>
            <a href="{{ path('app_register') }}" class="btn-primary ">Get Started</a>
        </div>
        
        <button class="mobile-menu-btn" id="mobileMenuBtn">☰</button>
    </div>
</nav>

<!-- Mobile Menu -->
<div class="mobile-menu" id="mobileMenu">
    <a href="{{ path('app_landing') }}"class="active">Home</a>
    <a href="{{ path('app_about') }}">About</a>
    <a href="{{ path('app_contact') }}">Contact</a>
    <a href="{{ path('app_login')}}">Login</a>
    <a href="{{ path('app_register')}}">Register</a>
</div>

<!-- Hero Section with Full Background Image -->
<section class="hero hero-bg">
    <div class="hero-overlay"></div>
    
    <div class="container hero-content">
        <div>
            <div class="hero-badge">🍺 Premium Craft Brewery</div>
            <h1>Crafting Excellence in Every Pour</h1>
            <p>Experience the finest artisanal beers brewed with passion and precision. Join thousands of beer enthusiasts who trust Samaco Brewery for quality and innovation.</p>
            <div class="hero-buttons">
                <a href="{{ path('app_register') }}" class="btn-primary">Start Your Journey</a>
                <a href="{{ path('app_about') }}" class="btn-secondary">Learn More</a>
            </div>
            <div class="trust-badge">
                <div class="avatars">
                    <div class="avatar">JD</div>
                </div>
                <span>Trusted by 2,000+ happy customers</span>
            </div>
        </div>
    </div>
</section>

<!-- Stats Section -->
<section class="stats">
    <div class="container">
        <div class="stats-grid">
            <div>
                <div class="stat-number">15+</div>
                <div class="stat-label">Years Experience</div>
            </div>
            <div>
                <div class="stat-number">50+</div>
                <div class="stat-label">Beer Varieties</div>
            </div>
            <div>
                <div class="stat-number">2K+</div>
                <div class="stat-label">Happy Customers</div>
            </div>
            <div>
                <div class="stat-number">25+</div>
                <div class="stat-label">Awards Won</div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="features">
    <div class="container">
        <h2 class="section-title">Why Choose Us</h2>
        <p class="section-subtitle">We're committed to delivering the finest craft beer experience with quality ingredients and sustainable practices.</p>
        
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">⭐</div>
                <h3>Premium Quality</h3>
                <p>Award-winning craft beers made with the finest ingredients and traditional brewing methods.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🌱</div>
                <h3>Sustainable</h3>
                <p>Eco-friendly brewing practices that protect our environment for future generations.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">👥</div>
                <h3>Community</h3>
                <p>Supporting local farmers and building strong partnerships within our community.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🚚</div>
                <h3>Fast Delivery</h3>
                <p>Quick and reliable delivery ensuring your beer arrives fresh and perfectly chilled.</p>
            </div>
        </div>
    </div>
</section>

<!-- Products Section -->
<section class="products">
    <div class="container">
        <h2 class="section-title">Featured Products</h2>
        <p class="section-subtitle">Discover our most popular craft beers, each with its own unique character and flavor profile.</p>
        
        <div class="products-grid">
            {% if products is defined and products|length > 0 %}
                {% for product in products|slice(0, 3) %}
                <div class="product-card">
                    <div class="product-image">
                        {% if 'Stout' in product.name or 'Dark' in product.name %}
                            🍻
                        {% elseif 'Light' in product.name or '0.0' in product.name %}
                            🥤
                        {% else %}
                            🍺
                        {% endif %}
                    </div>
                    <div class="product-content">
                        {% if product.stockQuantity > 400 %}
                            <span class="product-tag">BESTSELLER</span>
                        {% elseif product.price > 100 %}
                            <span class="product-tag">PREMIUM</span>
                        {% elseif product.stockQuantity < 100 %}
                            <span class="product-tag">LIMITED</span>
                        {% else %}
                            <span class="product-tag">POPULAR</span>
                        {% endif %}
                        <h3>{{ product.name }}</h3>
                        <p>{{ product.description|length > 80 ? product.description|slice(0, 80) ~ '...' : product.description }}</p>
                        <div class="product-footer">
                            <span class="price">₱{{ product.price|number_format(2) }}</span>
                        </div>
                    </div>
                </div>
                {% endfor %}
            {% else %}
                <div class="product-card">
                    <div class="product-image">🍺</div>
                    <div class="product-content">
                        <span class="product-tag">BESTSELLER</span>
                        <h3>Classic Lager</h3>
                        <p>A smooth, refreshing lager with notes of caramel and a crisp finish. Perfect for any occasion.</p>
                        <div class="product-footer">
                            <span class="price">₱89.00</span>
                        </div>
                    </div>
                </div>
                <div class="product-card">
                    <div class="product-image">🍻</div>
                    <div class="product-content">
                        <span class="product-tag">PREMIUM</span>
                        <h3>Dark Stout</h3>
                        <p>Rich and creamy stout with hints of coffee and chocolate. A true craft beer experience.</p>
                        <div class="product-footer">
                            <span class="price">₱120.00</span>
                        </div>
                    </div>
                </div>
                <div class="product-card">
                    <div class="product-image">🥤</div>
                    <div class="product-content">
                        <span class="product-tag">POPULAR</span>
                        <h3>Wheat Ale</h3>
                        <p>Light and citrusy wheat ale with a refreshing finish. Perfect for warm days.</p>
                        <div class="product-footer">
                            <span class="price">₱95.00</span>
                        </div>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
</section>

<!-- Process Section -->
<section class="process">
    <div class="container">
        <h2 class="section-title">Our Brewing Process</h2>
        <p class="section-subtitle">From selecting the finest ingredients to the final pour, every step is crafted with care.</p>
        
        <div class="process-grid">
            <div class="process-step">
                <div class="step-number">1</div>
                <h3>Selection</h3>
                <p>Carefully selecting premium hops, malt, and yeast from trusted suppliers.</p>
            </div>
            <div class="process-step">
                <div class="step-number">2</div>
                <h3>Brewing</h3>
                <p>Traditional brewing methods combined with modern technology for consistency.</p>
            </div>
            <div class="process-step">
                <div class="step-number">3</div>
                <h3>Fermentation</h3>
                <p>Patient fermentation process to develop complex flavors and aromas.</p>
            </div>
            <div class="process-step">
                <div class="step-number">4</div>
                <h3>Quality Check</h3>
                <p>Rigorous quality testing to ensure every batch meets our high standards.</p>
            </div>
        </div>
    </div>
</section>

<!-- Testimonials Section -->
<section class="testimonials">
    <div class="container">
        <h2 class="section-title">What Our Customers Say</h2>
        
        <div class="testimonials-grid">
            <div class="testimonial-card">
                <div class="quote-icon">"</div>
                <p>The best craft beer I've ever tasted! The Classic Lager has become my go-to drink for every weekend barbecue. Highly recommended!</p>
                <div class="testimonial-author">
                    <div class="author-avatar">MC</div>
                    <div>
                        <div class="author-name">Miguel Cruz</div>
                        <div class="author-title">Regular Customer</div>
                    </div>
                </div>
            </div>
            
            <div class="testimonial-card">
                <div class="quote-icon">"</div>
                <p>As a restaurant owner, I've tried many suppliers. Samaco Brewery consistently delivers quality, reliability, and excellent service.</p>
                <div class="testimonial-author">
                    <div class="author-avatar">AR</div>
                    <div>
                        <div class="author-name">Anna Reyes</div>
                        <div class="author-title">Restaurant Owner</div>
                    </div>
                </div>
            </div>
            
            <div class="testimonial-card">
                <div class="quote-icon">"</div>
                <p>The Dark Stout is absolutely phenomenal. Rich, creamy, and full of character. You can taste the craftsmanship in every sip.</p>
                <div class="testimonial-author">
                    <div class="author-avatar">DT</div>
                    <div>
                        <div class="author-name">David Tan</div>
                        <div class="author-title">Craft Beer Collector</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="cta">
    <div class="container">
        <h2>Ready to Experience the Best?</h2>
        <p>Join our community of beer lovers and get exclusive access to new releases, special offers, and brewing events.</p>
        <div class="cta-buttons">
            <a href="{{ path('app_register') }}" class="btn-white">Create Free Account</a>
            <a href="{{ path('app_contact') }}" class="btn-outline-white">Contact Us</a>
        </div>
    </div>
</section>

<!-- Footer -->
<footer class="footer">
    <div class="container">
        <div class="footer-grid">
            <div>
                <a href="{{ path('app_landing') }}" class="footer-logo">
                    <img src="{{ asset('images/logo.png') }}" alt="Samaco Brewery" style="height: 32px;">
                    <span>Samaco Brewery</span>
                </a>
                <p style="margin-top: 16px;">Crafting excellence in every pour since 2020.</p>
            </div>
            
            <div>
                <h4>Quick Links</h4>
                <ul class="footer-links">
                    <li><a href="{{ path('app_landing') }}">Home</a></li>
                    <li><a href="{{ path('app_about') }}">About</a></li>
                    <li><a href="{{ path('app_contact') }}">Contact</a></li>
                </ul>
            </div>
            
            <div>
                <h4>Account</h4>
                <ul class="footer-links">
                    <li><a href="{{ path('app_login') }}">Login</a></li>
                    <li><a href="{{ path('app_register') }}">Register</a></li>
                </ul>
            </div>
            
            <div>
                <h4>Contact</h4>
                <ul class="footer-links">
                    <li>123 Brewery Street</li>
                    <li>Manila, Philippines</li>
                    <li>info@samacobrewery.com</li>
                    <li>+63 123 456 7890</li>
                </ul>
            </div>
        </div>
        
        <div class="footer-bottom">
            <p>&copy; 2026 Samaco Brewery. All rights reserved.</p>
        </div>
    </div>
</footer>

<script>
    const mobileMenuBtn = document.getElementById('mobileMenuBtn');
    const mobileMenu = document.getElementById('mobileMenu');
    
    if (mobileMenuBtn && mobileMenu) {
        mobileMenuBtn.addEventListener('click', function() {
            mobileMenu.classList.toggle('active');
        });
        
        const mobileLinks = mobileMenu.querySelectorAll('a');
        mobileLinks.forEach(link => {
            link.addEventListener('click', function() {
                mobileMenu.classList.remove('active');
            });
        });
        
        window.addEventListener('resize', function() {
            if (window.innerWidth >= 768) {
                mobileMenu.classList.remove('active');
            }
        });
    }
</script>
{% endblock %}