{% extends 'layout.html.twig' %}

{% block title %}New Order - Samaco Brewery{% endblock %}

{% block dashboard_content %}
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
    
    <!-- Page Header -->
    <div class="mb-8">
        <nav class="flex items-center gap-2 text-sm text-gray-400 mb-6">
            <a href="{{ path('app_dashboard_home') }}" class="hover:text-yellow-400">Dashboard</a>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
            <a href="{{ path('app_order_index') }}" class="hover:text-yellow-400">Orders</a>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
            <span class="text-yellow-400 font-medium">New Order</span>
        </nav>
        
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
            <div>
                <h1 class="text-3xl md:text-4xl font-bold text-black mb-2">Create New Order</h1>
                <p class="text-gray-600">Create a new order for your brewery customers</p>
            </div>
            
            <a href="{{ path('app_order_index') }}"
               class="inline-flex items-center gap-2 px-5 py-3 text-sm font-semibold text-white bg-gray-700 rounded-xl hover:bg-gray-600 shadow-md">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
                Back to Orders
            </a>
        </div>
    </div>

    <div class="bg-gray-900/50 rounded-2xl border border-gray-700/50 shadow-xl p-6 md:p-8">
        <div class="mb-8">
            <h2 class="text-2xl font-bold text-black mb-2">Order Details</h2>
            <p class="text-gray-600">Fill in the details for your new order</p>
        </div>
        
        <div class="max-w-4xl mx-auto">
            {{ form_start(form, {'attr': {'class': 'space-y-8', 'id': 'order-form'}}) }}
                
                <div>
                    {{ form_label(form.customer, 'Customer', {'label_attr': {'class': 'block text-sm font-semibold text-yellow-500 mb-2'}})}}
                    {{ form_widget(form.customer, {'attr': {'class': 'w-full px-4 py-3.5 bg-gray-800/50 border border-gray-700 rounded-xl text-white'}})}}
                    {{ form_errors(form.customer) }}
                </div>

                <div class="bg-gray-800/30 rounded-2xl border border-gray-700/30 p-6">
                    <h3 class="text-xl font-bold text-black mb-2">Order Items</h3>
                    <p class="text-yellow-500 text-sm mb-4">⚠️ Stock will be deducted automatically when order is created.</p>
                    
                    <div id="order-items" class="space-y-4"></div>
                    
                    <div id="item-template" class="hidden">
                        <div class="order-item bg-gray-900/50 border border-gray-700/50 rounded-xl p-4">
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
                                <div>
                                    <label class="block text-sm text-gray-300 mb-2">Product</label>
                                    <select name="order[orderItems][__index__][product]" class="product-select w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg text-white">
                                        <option value="">Select Product</option>
                                        {% for product in products %}
                                            <option value="{{ product.id }}" data-price="{{ product.price }}" data-stock="{{ product.stockQuantity }}">
                                                {{ product.name }} - ${{ product.price|number_format(2) }} (Stock: {{ product.stockQuantity }})
                                            </option>
                                        {% endfor %}
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-sm text-gray-300 mb-2">Quantity</label>
                                    <input type="number" name="order[orderItems][__index__][quantity]" class="quantity-input w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg text-white" min="1" value="1">
                                </div>
                                <div>
                                    <label class="block text-sm text-gray-300 mb-2">Unit Price</label>
                                    <input type="text" name="order[orderItems][__index__][unitPrice]" class="unit-price-input w-full px-4 py-3 bg-gray-800/30 border border-gray-700 rounded-lg text-gray-400" readonly value="0.00">
                                </div>
                            </div>
                            <button type="button" class="remove-item text-red-400 hover:text-red-300 text-sm">Remove Item</button>
                        </div>
                    </div>
                    
                    <button type="button" id="add-first-item" class="mt-3 text-yellow-500 hover:text-yellow-400">+ Add First Item</button>
                    <button type="button" id="add-item" class="hidden mt-3 text-yellow-500 hover:text-yellow-400">+ Add Another Item</button>
                </div>

                <div class="bg-yellow-900/20 border border-yellow-800/30 rounded-2xl p-6">
                    <div class="flex justify-between items-center">
                        <h3 class="text-lg font-bold text-yellow-300">Order Total</h3>
                        <div class="text-3xl font-bold text-white">$<span id="order-total">0.00</span></div>
                    </div>
                </div>

                <div>
                    {{ form_label(form.status, 'Order Status', {'label_attr': {'class': 'block text-sm font-semibold text-yellow-500 mb-2'}})}}
                    {{ form_widget(form.status, {'attr': {'class': 'w-full px-4 py-3.5 bg-gray-800/50 border border-gray-700 rounded-xl text-white'}})}}
                </div>

                <div>
                    {{ form_label(form.notes, 'Order Notes', {'label_attr': {'class': 'block text-sm font-semibold text-yellow-500 mb-2'}})}}
                    {{ form_widget(form.notes, {'attr': {'class': 'w-full px-4 py-3.5 bg-gray-800/50 border border-gray-700 rounded-xl text-white', 'rows': 4, 'placeholder': 'Add any special instructions...'}})}}
                </div>

                <input type="hidden" id="items-count" name="items-count" value="0">

                <div class="flex gap-4 pt-4">
                    <button type="submit" class="bg-yellow-500 text-black font-bold px-8 py-4 rounded-xl hover:bg-yellow-400">Create Order</button>
                    <a href="{{ path('app_order_index') }}" class="bg-gray-600 text-white font-bold px-8 py-4 rounded-xl hover:bg-gray-500 text-center">Cancel</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    var itemCount = 0;
    
    function getStockData() {
        var stock = {};
        var options = document.querySelectorAll('.product-select option');
        for (var i = 0; i < options.length; i++) {
            var opt = options[i];
            if (opt.value) {
                stock[opt.value] = parseInt(opt.getAttribute('data-stock')) || 0;
            }
        }
        return stock;
    }
    
    function calculateTotal() {
        var total = 0;
        var items = document.querySelectorAll('.order-item');
        for (var i = 0; i < items.length; i++) {
            var qty = parseFloat(items[i].querySelector('.quantity-input').value) || 0;
            var price = parseFloat(items[i].querySelector('.unit-price-input').value) || 0;
            total = total + (qty * price);
        }
        var totalSpan = document.getElementById('order-total');
        if (totalSpan) totalSpan.innerText = total.toFixed(2);
    }
    
    function checkStock(productSelect, quantityInput) {
        var stockData = getStockData();
        var productId = productSelect.value;
        var available = stockData[productId] || 0;
        var requested = parseInt(quantityInput.value) || 0;
        
        if (requested > available && available > 0) {
            alert('Not enough stock! Only ' + available + ' units available for ' + productSelect.options[productSelect.selectedIndex].text.split(' -')[0]);
            quantityInput.value = available;
            calculateTotal();
        }
    }
    
    function addItemEventListeners(item) {
        var productSelect = item.querySelector('.product-select');
        var quantityInput = item.querySelector('.quantity-input');
        var unitPriceInput = item.querySelector('.unit-price-input');
        var removeBtn = item.querySelector('.remove-item');
        
        if (productSelect) {
            productSelect.addEventListener('change', function() {
                var opt = this.options[this.selectedIndex];
                var price = opt.getAttribute('data-price') || '0';
                if (unitPriceInput) unitPriceInput.value = parseFloat(price).toFixed(2);
                if (quantityInput) quantityInput.value = 1;
                calculateTotal();
            });
        }
        
        if (quantityInput) {
            quantityInput.addEventListener('input', function() {
                checkStock(productSelect, this);
            });
        }
        
        if (removeBtn) {
            removeBtn.addEventListener('click', function() {
                item.remove();
                calculateTotal();
                var itemsLeft = document.querySelectorAll('.order-item').length;
                var addFirstBtn = document.getElementById('add-first-item');
                var addItemBtn = document.getElementById('add-item');
                if (itemsLeft === 0) {
                    if (addFirstBtn) addFirstBtn.style.display = 'inline-block';
                    if (addItemBtn) addItemBtn.style.display = 'none';
                }
            });
        }
    }
    
    var addFirstBtn = document.getElementById('add-first-item');
    var addItemBtn = document.getElementById('add-item');
    var orderItems = document.getElementById('order-items');
    var template = document.getElementById('item-template');
    
    if (addFirstBtn) {
        addFirstBtn.addEventListener('click', function() {
            var html = template.innerHTML.replace(/__index__/g, itemCount);
            var div = document.createElement('div');
            div.innerHTML = html;
            var newItem = div.firstElementChild;
            if (orderItems) orderItems.appendChild(newItem);
            addItemEventListeners(newItem);
            itemCount = itemCount + 1;
            this.style.display = 'none';
            if (addItemBtn) addItemBtn.style.display = 'inline-block';
            calculateTotal();
        });
    }
    
    if (addItemBtn) {
        addItemBtn.addEventListener('click', function() {
            var html = template.innerHTML.replace(/__index__/g, itemCount);
            var div = document.createElement('div');
            div.innerHTML = html;
            var newItem = div.firstElementChild;
            if (orderItems) orderItems.appendChild(newItem);
            addItemEventListeners(newItem);
            itemCount = itemCount + 1;
            calculateTotal();
        });
    }
    
    var orderForm = document.getElementById('order-form');
    if (orderForm) {
        orderForm.addEventListener('submit', function(e) {
            var items = document.querySelectorAll('.order-item');
            
            // Filter out empty items (no product selected)
            var validItems = [];
            for (var i = 0; i < items.length; i++) {
                var productSelect = items[i].querySelector('.product-select');
                if (productSelect && productSelect.value) {
                    validItems.push(items[i]);
                }
            }
            
            if (validItems.length === 0) {
                e.preventDefault();
                alert('Please add at least one item to the order.');
                return false;
            }
            
            var stockData = getStockData();
            var productQuantities = {};
            
            // Sum up quantities for each product across all items
            for (var i = 0; i < validItems.length; i++) {
                var item = validItems[i];
                var productSelect = item.querySelector('.product-select');
                var quantityInput = item.querySelector('.quantity-input');
                var productId = productSelect.value;
                var quantity = parseInt(quantityInput.value) || 0;
                
                if (productQuantities[productId]) {
                    productQuantities[productId] = productQuantities[productId] + quantity;
                } else {
                    productQuantities[productId] = quantity;
                }
            }
            
            // Check stock for each product (total quantity)
            var hasError = false;
            var errorMessage = '';
            
            for (var productId in productQuantities) {
                var available = stockData[productId] || 0;
                var requested = productQuantities[productId];
                
                if (requested > available) {
                    // Find the product name
                    var productName = '';
                    for (var i = 0; i < validItems.length; i++) {
                        var productSelect = validItems[i].querySelector('.product-select');
                        if (productSelect.value == productId) {
                            productName = productSelect.options[productSelect.selectedIndex].text.split(' -')[0];
                            break;
                        }
                    }
                    hasError = true;
                    errorMessage = errorMessage + 'Not enough stock for ' + productName + '. Requested: ' + requested + ', Available: ' + available + '\n';
                }
            }
            
            if (hasError) {
                e.preventDefault();
                alert('Stock validation failed:\n' + errorMessage);
                return false;
            }
            
            return true;
        });
    }
});
</script>
{% endblock %}